# requirements.txt

# PDF, DOCX, PPTX, HTML, CSV, XLSX support
pdfplumber
PyMuPDF
python-docx
python-pptx
beautifulsoup4
lxml
openpyxl
pandas

# Email, eBooks, ODF
extract-msg
ebooklib
odfpy

# OCR
pytesseract
Pillow

# PII Detection
# Extra "[transformers]" includes the necessary dependencies for the
# `TransformersNlpEngine` class used in `scanner/cc_detector.py`.
presidio-analyzer[transformers]>=2.2
presidio-anonymizer
spacy

# python -m spacy download en_core_web_lg

# Utility
regex
tqdm
psutil

# Added from the code block
transformers
torch

pandas
streamlit

# Added from the code block
paramiko